# Local Paper Cache

This directory stores downloaded PDFs for local reading. PDF files are ignored
by git until Git LFS or another artifact strategy is selected.

Tracked metadata lives in:

- `../source-log.md`
- `../references.bib`
